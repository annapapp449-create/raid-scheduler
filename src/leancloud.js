/**
 * LeanCloud 初始化配置
 *
 * Phase 2 接入 LeanCloud 时填写以下凭证：
 * 1. 前往 https://console.leancloud.cn/ 注册
 * 2. 创建应用 → 开发版
 * 3. 设置 → 应用凭证 → 获取 AppID / AppKey / REST API 服务器地址
 * 4. 设置 → 安全中心 → Web 安全域名 → 添加 localhost + 正式域名
 */

// import AV from 'leancloud-storage';

// AV.init({
//   appId: 'YOUR_APP_ID',
//   appKey: 'YOUR_APP_KEY',
//   serverURL: 'YOUR_SERVER_URL',
// });

// export default AV;

export default null;
